apiVersion: v1
kind: Service
metadata:
  name: realstate-app-service
  nameSpace: prod
  labels:
    env: prod
spec:
  type: ClusterIP
  selcetor:
      app: realstate
  ports:
    - targetPort: 80
      port: 80
      protocol: TCP

